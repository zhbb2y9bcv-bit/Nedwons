# Risk Register

This register is the project's honesty ledger. Every unresolved assumption, accepted
risk, untested control, platform limitation, and external-audit requirement lives here.
It is a launch gate: no entry may be silently dropped. Status values are
`OPEN`, `MITIGATING`, `ACCEPTED` (with named owner), or `CLOSED` (with evidence link).

Last updated: 2026-07-18. Owner of this file: security architect.

## Legend

- **Severity**: Critical / High / Medium / Low — impact if the risk is realized.
- **Owner**: role accountable for driving the item to closure or formally accepting it.

## R-000 — Overall maturity

| Field | Value |
|-------|-------|
| Severity | High |
| Status | OPEN |
| Owner | security architect |

The product is at Milestone 0 + the first tested slice of Milestone 1. It is **not**
production-ready and must not be represented as such. The claims allowed today are only
those backed by tests in this repository. Release acceptance criteria (see
[RELEASE_CHECKLIST.md](RELEASE_CHECKLIST.md) §"Release acceptance") are almost entirely
unmet.

## Untested / unavailable controls (this environment)

| ID | Risk | Sev | Status | Owner | Note |
|----|------|-----|--------|-------|------|
| R-101 | On-device Secure Enclave enrollment, App Attest, Keychain ACLs, biometrics, and the `@main` app target are **not** verified on a device/simulator. | High | MITIGATING | iOS lead | Reduced 2026-07-17: the Swift `SentinelClient` is verified against the **live** Rust backend over real HTTP (`scripts/swift_backend_smoke.sh` → SMOKE_OK), including the INV-2 negative check. **Further reduced 2026-07-17 (R-G0-2):** the app now selects the Enclave signer via `DeviceIdentity` with a **fail-closed** no-hardware policy and **persists/reloads the enrolled key** (login signs the same key — previously it signed a fresh key each launch); proven headlessly by `DeviceIdentityTests`. **Further reduced 2026-07-18 (ADR-0007):** the **UniFFI MLS binding is now implemented and proven headlessly** — `core/mls-ffi` (thin FFI shim over `mls-core`), a reproducible `MlsFfi.xcframework` (macOS + iOS device + iOS-sim slices via `scripts/build_mls_ffi.sh`), and a **Swift↔Rust integration test** in which two Rust-backed clients exchange a real MLS message, persist, relaunch, retry, and reject hostile input (`apps/ios/SentinelMLS`, host/macOS slice). The **simulator slice now RUNS the full bridge suite in the iOS simulator** (`scripts/test_mls_sim.sh`, in CI); the device slice compiles/links (compile-only). So the binding no longer blocks. Still needs real hardware: Enclave key non-exportability, background-refresh-without-biometric, Keychain `ThisDeviceOnly` backup exclusion, **App Attest (still unwritten)**, the `@main` Xcode target, and **running** the MLS bridge on a physical device. Steps + assertions in `docs/GATE1_DEVICE_CHECKLIST.md`. |
| R-102 | Postgres atomic challenge consumption and refresh rotation. | High | **CLOSED** | backend lead | **Closed 2026-07-17.** `services/api` implements the store traits over PostgreSQL 17; `services/api/tests/pg_invariants.rs` proves the atomicity contracts against a real DB, including two true-concurrency race tests (challenge consume = exactly one winner; refresh rotate = at most one winner, reuse burns the family) and the single-active-device partial-unique-index. `http_api.rs` drives the full flow end to end. 30 tests pass. |
| R-103 | Docker/`infra` deployment definitions unvalidated. | Medium | **MITIGATING** | ops | Docker (Colima) installed 2026-07-17. `docker-compose config` validates; the `postgres:17` service starts, is healthy, and tears down. The in-container Rust image build was not run (time); Dockerfile is standard multi-stage. |
| R-305 | Password blocklist is a small embedded list, not a real compromised-credential corpus. | Medium | **MITIGATING** | backend lead | **Implemented 2026-07-18.** The **k-anonymity range-query protocol** (NIST SP 800-63B-4 / HIBP) is implemented and tested in `auth_core::breach` — `SHA-1(password)`, split into a 5-char prefix + 35-char suffix, membership decided locally against suffixes returned for the prefix, so the full password/hash never leaves the caller (proven: the `RangeProvider` is only ever asked for a 5-char prefix). Wired into registration via `AuthService::with_breach_provider` (rejects a breached password that passes the length policy; **fails open** on a provider outage so an external hiccup can't block sign-up). A **bundled `StaticCorpus`** of common breached passwords ships as the default `RangeProvider`. **Residual (keeps this MITIGATING):** the default corpus is small — production swaps the same `RangeProvider` for a large corpus (an HTTP HIBP range query or a Bloom filter). **HTTP provider progressed 2026-07-18:** `sentinel_api::breach_http` implements the HIBP range shape — `hibp_range_url` + `parse_hibp_range` (pure, tested: CRLF/count stripping, junk-line rejection, case normalization) + `HttpRangeProvider` whose fetch is an **injected closure**, so no HTTP-client dependency is added to the auth path yet; binding a concrete HTTPS client (strict timeout, fail-open) is a deliberate supply-chain step left for review. Now covered by the password-change flow too (landed 2026-07-18). |
| R-306 | Rate limiting is per-IP in-process (single instance). Multi-instance deployments need a shared/distributed limiter, and abuse defense needs layered per-account/global limits. | Medium | MITIGATING | backend lead | `governor` GCRA limiter per instance. **Updated 2026-07-17:** trusted-proxy client-IP extraction implemented (`SENTINEL_TRUSTED_IP_HEADER`, `build_router_cfg`) — opt-in only; a client-supplied header is ignored by default so it can't be spoofed, and each real client IP gets its own bucket behind a proxy. Tested in `http_api.rs` (trusted-header keying + header-ignored-without-trust). Still OPEN: layered per-account/global limits and a distributed limiter for multi-instance (ABUSE_MODEL.md). |
| R-307 | Access tokens are opaque random values stored server-side (hashed). This is correct but adds a DB read per authenticated request; a short-lived signed token (PASETO/JWT) may be preferable at scale. | Low | ACCEPTED | backend lead | Acceptable for v1; revisit if the session-store read becomes a bottleneck. Revocation is simpler with server-side tokens. |
| R-104 | The "server never sees plaintext" invariant (INV-1). | Critical | **CLOSED (Rust side)** | crypto integrator | **Closed 2026-07-17.** OpenMLS integrated in `core/mls-core`; `tests/e2ee.rs` proves encrypt/decrypt, that ciphertext contains no plaintext, outsiders can't decrypt, and removed members can't read future epochs. `services/api/tests/relay_e2ee.rs` routes REAL MLS ciphertext through the HTTP relay and a **direct query of the `envelopes` table confirms no plaintext at rest**. The server library does not link `mls-core`. Residual: proven in Rust; the on-device Swift path (via UniFFI) is verified in Section 3 / on device (R-101). |
| R-105 | **Client durable-blob growth is unbounded and every commit rewrites the whole blob.** `mls-core::durable` persists the message log (`messages`), dedup set (`seen_inbound`), and outbox inside ONE atomically-committed encrypted blob — correct for crash-safety, but serialization cost is O(total history) per operation and grows forever, so a long-lived heavy conversation degrades linearly (slow commits, memory, battery) and the dedup set never shrinks. Identified 2026-07-18 during the FFI-bridge review. | Medium | OPEN | crypto integrator | The atomic-coupling *invariant* (ratchet + message state advance together) must be preserved by any fix. Planned fix is the remaining Gate 2 item: an **encrypted on-device DB** where the hot MLS-state blob stays small and message history moves to paginated, individually-encrypted rows committed in the same transaction; plus bounded retention for `seen_inbound` (e.g. a high-watermark + window, safe because server envelope ids are monotonic and acked mail is deleted server-side). Interim mitigation 2026-07-18: the FFI exposes `message_count`/`messages_page` so at least *marshalling* is bounded; storage is not yet. **Dedup bounded 2026-07-18:** `seen_inbound` is now a `dedup_watermark` (contiguous seen prefix collapsed into one integer) + a hard-capped out-of-order tail (`MAX_SEEN_ABOVE_WATERMARK`), so the dedup component no longer grows without limit and per-commit serialization of it is O(window) not O(history) — proven by unit tests (contiguous collapse, gap retention, permanent-gap hard bound); backward-compatible via `#[serde(default)]` (old blobs self-heal on next commit). Safe because OpenMLS's ratchet is the real replay guard. **Still OPEN:** the `messages` history vector is still rewritten whole per commit — the encrypted on-device DB (paginated rows) remains the fix. Not a launch blocker for small betas; IS a blocker for "millions of users" claims. |

## Cryptography & protocol

| ID | Risk | Sev | Status | Owner | Note |
|----|------|-----|--------|-------|------|
| R-201 | Key transparency: a malicious/compelled server could substitute a device/identity key. | High | MITIGATING | crypto integrator | **Updated 2026-07-18 (terminology corrected).** An **RFC 6962-compatible append-only Merkle-log primitive inside an application-specific KT design** is implemented and tested: bindings logged at enrollment, signed tree heads (ECDSA-P256), inclusion + consistency proofs, and **client self-monitoring** (`auth_core::transparency`, `services/api/src/transparency.rs`, `SentinelKit/Transparency.swift`; the live smoke self-monitors end to end). This detects logged-key substitution and history rewriting. **Precision required:** only the *tree construction* is standard (and RFC 9162 obsoletes RFC 6962 for CT itself); the leaf schema, enrollment coupling, log-key pinning, and client policy are **ours** and need specialized review — do not describe this as "standardized KT" or "not hand-rolled" on the strength of the tree alone. **Still OPEN (see `docs/KEY_TRANSPARENCY.md`):** split-view/equivocation resistance needs gossip/witnessing; efficient verifiable non-inclusion needs a verifiable map (CONIKS/Parakeet); a specialised external audit; atomic enroll+log/reconciliation; and **log signing-key rotation + client checkpoint policy**. **Device-removal auditability in progress:** today only device *additions* are logged, so a server could **silently revoke** a device with no signed trace; ADR-0013 designs a kind-tagged **leaf schema v2** (disjoint Binding/Revocation leaves, coexisting with legacy v1 in the append-only log) to close it. **Slices 1–2 landed 2026-07-18** — Slice 1: the pure v2 leaf codec (`encode_binding_v2`/`encode_revocation_v2`/`decode_leaf_v2`, golden vectors + strict-parse/disjointness/legacy-rejection tests). Slice 2: `revoke_device_handler` now appends a v2 **revocation leaf** best-effort; the account view surfaces `revoked_at` per leaf (omitted for bindings, so shipped clients are unaffected); end-to-end tested (`device_revocation_is_logged_in_transparency`) — the revocation is logged AND the original binding leaf stays present+unmarked so the existing self-monitor is undisturbed. **Slice 3 landed 2026-07-18** — `SentinelClient.monitorDeviceRevocations` fetches the STH + account view, verifies the STH signature under the **pinned** log key and each revocation leaf's inclusion proof (fail-closed via `verificationFailed`), and returns the verified revocations; the app compares them against revocations it initiated and flags any it did not (a silent removal). `revocationLeaves(in:)` + `revoked_at` decoding are unit-tested. **Device removals are now auditable end-to-end** (logged server-side, verified client-side) — closing the "silent revocation" gap noted here. (A revoked device's own token is dead, so the monitor runs from another live device of the account.) New binding appends stay v1 (schema uniformity only, no security benefit). Until those land, **manual safety-number verification remains the primary guarantee** and the "server cannot substitute keys" claim must not be made. |
| R-202 | OpenMLS 0.8.1 (MIT) selected but its audit history and our specific usage have not had an independent cryptographic review. | High | OPEN | crypto integrator | External crypto audit required pre-launch. See ADR-0001. |
| R-203 | Post-quantum path (hybrid) is documented as a direction, not implemented. MLS ciphersuite in use is classical. | Medium | ACCEPTED | crypto integrator | Acceptable for v1 provided we do not advertise PQ security. Revisit when standardized MLS PQ ciphersuites are available in OpenMLS. |
| R-204 | Sealed-sender / metadata-minimization; the relay sees routing metadata (sender/recipient device, timing). | Medium | OPEN | backend lead | **Design decided + foundation landed 2026-07-18 (ADR-0012).** Signal-style sealed sender chosen; the **verifiable sender certificate** is implemented and **cross-language tested** — `auth_core::sender_cert` (canonical encode + verify-with-expiry) and `SentinelKit.SenderCertificate` (byte-identical, golden vector), so a recipient can verify who sent a message without the relay learning it. **Issuance landed 2026-07-18:** `GET /v1/sender-certificate` (authed) issues a 24h signed certificate for the caller's device under a dedicated `SENTINEL_SENDER_CERT_KEY` (distinct from auth/transparency keys; ephemeral in dev), returning the cert public key for client pinning — integration-tested (signature verifies under the returned key, binds the device's own public key, tampered key fails); the relay stays MLS-blind. **Recipient key-match verification landed 2026-07-18:** `SenderCertificate.verifySealedSender` ties a relay-invisible sender to the actual MLS sender (sig+expiry+bound-key==MLS-key), unit-tested. **Delivery abuse model now specified (ADR-0014, Proposed 2026-07-18):** delivery access keys (recipient registers `V_r=H(K_r)`, distributes `K_r` over E2EE, sender presents it to an *unauthenticated* `POST /v1/sealed/deliver`; relay learns `K_r` = spam-gate only, authenticity backstopped by the cert check), recipient-side block enforcement + block→rotate, per-recipient rate limits, a separate `sealed_envelopes` table (no `sender_device`/`conversation_id`; idempotency re-scoped to `(recipient_device, key)`), client-side fan-out, 2a→2d rollout. **Slices 2a + 2b landed 2026-07-18 (2b user-greenlit after a review pass):** 2a — DAK primitive + `PUT /v1/delivery-access-key` + migration V16. 2b — the **unauthenticated `POST /v1/sealed/deliver`** endpoint: verifies the presented `K_r` against the recipient's verifier (timing-equalized constant-time compare; uniform `403` for unknown-device/no-verifier/bad-key = no existence oracle), then stores a **`sealed_envelopes`** row (migration V17) with **NO `sender_device` and NO `conversation_id`** — proven by an integration test that delivers, reads (sealed flag, null sender), dedups, and acks via `sealed_ids`; per-recipient rate limit counts only *authorized* deliveries; sealed retention purge wired. Identified delivery + WebSocket streaming verified untouched (full suite green). **Slice 2c transport half landed 2026-07-18:** `SentinelKit.DeliveryAccessKey` (CSPRNG `K_r`, SHA-256 verifier pinned cross-language) + `registerDeliveryAccessKey` (only the verifier leaves the device, test-asserted) + `deliverSealed` (NO Authorization header, test-asserted; `K_r` in `X-Delivery-Key`; client-side fan-out; uniform 403 typed) + sealed-aware `InboxEnvelope`/`ackInbox(sealedIds:)` (fixing the required-fields decode that would have broken the inbox on the first sealed envelope). **Still OPEN (keeps R-204 MITIGATING, not CLOSED):** the 2c app-flow half — `K_r` distribution over E2EE, recipient-side block-drop after `verifySealedSender`, block→rotate-and-redistribute, message-request fallback (all need the app/AppModel flows) — plus WebSocket-sealed (per-source watermark) and padding/cover-traffic (2d). The relay learning `K_r` (spam-gate only) is the documented, accepted limit. Do NOT advertise metadata privacy beyond what ships (PRIVACY.md). |
| R-506 | **Server routing membership and cryptographic (MLS) membership are disconnected** (Gate 0 R-G0-5). The relay's `conversation_members` is an independent source of truth and there is no server-side MLS group yet, so a routing change need not correspond to a cryptographic commit. Also: no in-message protocol version on envelopes/control messages. | High | **MITIGATING** | crypto integrator + backend lead | **Updated 2026-07-18 — ADR-0010 chosen + reference implementation landed.** Membership can now flow through **device-signed manifests bound to the opaque commit hash** (`POST /v1/conversations/{id}/commit`): the MLS-blind relay verifies signature (enrolled device key) + governance (ADR-0009) + a per-group **epoch compare-and-swap** (one commit per transition, no forks) + hash binding, and applies routing delta + commit/welcome fan-out + removed-device delivery cutoff + append-only audit atomically; recipient clients run the **commit↔manifest correspondence check** (`mls_core::process_commit_checked`) and refuse a lying commit unmerged. Proven by a **headless multi-client simulation of real MLS clients through the real relay** (`services/api/tests/membership_sim.rs`): happy path, concurrent-commit race (one winner), the lying-manifest limitation caught by the recipient, atomic remove-with-cutoff, governance/signature/idempotency, expiry/canonical rejection. Control messages are versioned by the manifest domain tag (`app.sentinel.membership.v1`). **Client wiring landed 2026-07-18:** the proposer flow is real and safe — `mls_core` **staged commits** (build without merging; `merge_staged` only after the server's epoch CAS confirms, `clear_staged` on rejection, so a race loser never desyncs), exposed through `mls-ffi` (`stage_add`/`stage_remove`/`merge_staged`/`clear_staged`/`process_commit`) and driven by `SentinelKit` (`commitMembership`/`conversationEpoch`/`membershipEvent`) with a **byte-identical `MembershipManifest` encoder** (shared vector, cross-language golden) so the device signs exactly what the relay verifies. The recipient path fetches `GET …/membership/{epoch}` and runs the correspondence check. Proven by a **real two-admin staged race** in the simulation (loser discards, catches up via manifest fetch + correspondence check, rebases). **Hardened 2026-07-18:** (a) **recipient signature verification** landed — `SentinelClient.verifyIncomingMembershipEvent` verifies the manifest's device signature against the actor's **transparency-logged** key (STH under the pinned log key + inclusion proof), not a server-asserted key (migration V11 exposes `actor_account`); (b) a **migration enforcement flag** landed — `mls_authoritative` conversations (opt-in at create, migration V12) refuse EVERY legacy routing mutation (direct add/remove/leave, invite creation, join-approve) with `409 commits_required`, so an authoritative conversation can only change membership through `/commit`; proven by `membership_sim.rs`. **Still OPEN (keeps this MITIGATING, not CLOSED):** authoritative membership is opt-in — the **default is still legacy** and existing conversations/flows are not yet migrated (flip the default once the invite/join UX is rebuilt as admin add-commits); **application-envelope** protocol versioning is still unversioned; the `@main` app screens driving this are R-101; and the honest residual — a *valid member's* lying manifest is accepted server-side and only caught by clients (content via the correspondence check, signature via the transparency-anchored check) — is inherent to an MLS-blind relay (see ADR-0010). **Invariant (must hold):** the API routing set must not silently diverge from the set of MLS clients holding the current group epoch — but a **strictly MLS-blind relay cannot independently prove** that a claimed routing delta matches an opaque MLS commit. **Updated 2026-07-18:** the client MLS core is now bridged to Swift (ADR-0007), so this is unblocked for design (not implementation this arc). A **future ADR** must compare: (1) a server-verifiable public MLS control/commit path — and whether it violates the no-MLS-link boundary (ADR-0001); (2) a canonical **device-signed membership manifest bound to the opaque commit hash**, client-verified, with an honest statement of what the server still cannot prove; (3) a delivery design that does not treat the plaintext routing table as the authoritative security boundary. The chosen protocol must define group id, prev/next epoch, protocol version, control type, opaque commit hash, manifest hash, actor device, authorization role, resulting device set/delta, idempotency id, and expiry, with an atomic DB compare-and-swap; and cover concurrent commits, stale epochs, rejected-commit cleanup, forks, removed-device cutoff, Welcome delivery, retries, rollback, and mismatch recovery. **Do not select an option before the ADR.** |
| R-507 | Social/profile metadata (usernames, display names, bios, friend graph, group membership) is **server-readable plaintext** (Gate 0 R-G0-4). Correct for routing/search today, but "the server stores only ciphertext" must not be generalized to metadata. | Medium | MITIGATING | backend lead + product | Message *content* privacy is proven (INV-1, R-104). **Updated 2026-07-17:** PRIVACY.md now lists each plaintext metadata field in the data table and adds an explicit "Content vs. metadata — be precise" section forbidding over-claiming. Still OPEN: implementing E2EE profile fields shared post-connection + metadata minimization (Gate 3 / R-204). |

## Authentication & accounts

| ID | Risk | Sev | Status | Owner | Note |
|----|------|-----|--------|-------|------|
| R-301 | App Attest / integrity verdicts are defense-in-depth risk signals, **bypassable**, and not a substitute for device-key proof-of-possession. A jailbroken device can defeat them. | Medium | MITIGATING | iOS lead | Mandatory control is the device-key signature (implemented + tested in auth-core). Integrity is tiered/advisory. |
| R-302 | Argon2id parameters must be benchmarked + tuned on production hardware. | Medium | MITIGATING | backend lead | **Benchmark harness provided 2026-07-18.** `cargo run --release -p auth-core --example argon2_bench` measures per-hash wall-clock of the configured params on the target host; the params are exposed (`password::{MEMORY_KIB,ITERATIONS,PARALLELISM,argon2_params}`) and the tuning methodology + target (~0.25–0.5 s/hash) is documented in CRYPTOGRAPHY.md §Argon2 tuning. The committed values are an OWASP-aligned FLOOR (measured ~12 ms on a dev laptop — far too cheap), so the operator MUST run the bench on prod hardware, raise the params, and record the chosen values + a version. Rehash-on-verify applies a raise on next login. |
| R-303 | Server-side pepper (KMS/HSM) for credential hashing. | Medium | MITIGATING | ops | **Implemented 2026-07-18.** `AuthService::with_pepper` mixes a server-side secret (Argon2 keyed "secret", `password::hasher_with_pepper` via `Argon2::new_with_secret`) into ALL password + recovery-secret hashing, so a database-only compromise cannot offline-crack credentials. Wired in `main.rs` behind `SENTINEL_PASSWORD_PEPPER` (opt-in; leaked to `'static` as a process secret; the timing-dummy hash is rebuilt under the same pepper so enumeration resistance holds). Tested: same password + stores under a DIFFERENT pepper fails to verify. **Residual:** the pepper must come from a real KMS/HSM (env is a placeholder); enabling/rotating it invalidates existing hashes (set before users exist, or add rehash-on-next-login). |
| R-304 | Recovery flow (trusted-device approval / recovery kit) — account recovery abuse surface. | High | **MITIGATING** | backend lead | **Implemented 2026-07-18 (ADR-0003 recovery-secret path).** A high-entropy recovery secret, **set while authenticated** (`POST /v1/recovery/set`) and stored **only** as an Argon2id hash, authorizes enrolling a NEW device with no other device present (`POST /v1/recover/begin`+`/finish`): the ceremony verifies the secret AND the new device's proof-of-possession, then provisions the new device a session (reusing the ADR-0008 device-add machinery + cap). `recover_begin` is **enumeration-resistant** (always returns a challenge; one Argon2 verify on every finish, real or dummy, so timing doesn't leak account/secret existence). Recovery restores **account access, not E2EE history** — the new device has a fresh MLS identity and is re-added to conversations by members via commits; nothing is silently restored, and the trusted-device path is just enrollment (ADR-0008). Tested: `auth-core/tests/invariants.rs` (recover works / wrong-secret / no-secret / too-short-at-setup) and `services/api/tests/recovery.rs` (end-to-end restore + revoke the lost device; wrong/unset/unknown refused). **Still OPEN (keeps this MITIGATING):** per-account recovery-attempt lockout is DONE (5 failures -> 15-min cooldown, migration V15); an optional time-delay/cancel window; **notifications** to other devices on recovery (needs push, R-502); recording recovery events in the transparency log; and no external review (R-503). Per-IP rate limiting (R-306) applies today. |
| R-308 | **Access tokens are bearer credentials, not sender-constrained.** An access token exfiltrated from a compromised client, a TLS-terminating middlebox, or an accidentally-logged header could be **replayed from any device** until it expires or is revoked. **Verified in code 2026-07-18 (Phase 5).** | High | **MITIGATING** | backend lead | **Implemented 2026-07-18 (ADR-0011).** Sender-constraining per **RFC 9449 (DPoP)** semantics is built: `auth_core::request_proof` defines a device-key-signed transcript binding method + path + SHA-256(access-token) + timestamp + a single-use nonce; the api `proof_layer` middleware (behind `SENTINEL_REQUIRE_PROOF`) requires + verifies a proof on every request bearing an `Authorization` token, under the token's device enrolled key, with a ±60 s freshness window and a per-instance single-use `ProofReplayCache`. A stolen token alone is now inert. `SentinelKit.RequestProof` produces the proof header (byte-identical encoder, cross-language golden). Tested: `services/api/tests/dpop.rs` (proof required; valid accepted; replay/wrong-path/wrong-method/stale/wrong-key refused; fresh nonce works; unauth endpoints unaffected). **Still OPEN (keeps this MITIGATING):** enforcement is **opt-in and off by default** during migration (not closed until on-by-default + the client always signs); the replay cache is **per-instance** (multi-instance needs a shared cache or server-issued nonces, R-306-style); per-frame WebSocket proofs are out of scope (the upgrade is proofed); no external review (R-503). Base facts unchanged: the **refresh** path was already sender-constrained; access TTL is short with server-side revocation. |

## Platform & store

| ID | Risk | Sev | Status | Owner | Note |
|----|------|-----|--------|-------|------|
| R-401 | Apple mandates the iOS 26 SDK + Xcode 26 for App Store uploads (effective 2026-04-28). We build with Xcode 26.6 — compliant today, but this is a moving target. | Low | MITIGATING | release eng | Re-verify each cycle. Source: developer.apple.com/news/upcoming-requirements (accessed 2026-07-17). |
| R-402 | Privacy manifest, required-reason API declarations, and App Store privacy disclosures are drafted (PRIVACY.md) but not finalized against the shipped code. | High | OPEN | iOS lead | Disclosures must match actual behavior; mismatch is a rejection and a trust failure. |
| R-403 | Legal compliance (GDPR/UK GDPR/CCPA-CPRA, children's privacy, export/sanctions, data residency, law-enforcement response) is **not** determined by code and requires qualified counsel. | High | OPEN | legal (external) | This project cannot self-certify legal compliance. |

## Supply chain & operations

| ID | Risk | Sev | Status | Owner | Note |
|----|------|-----|--------|-------|------|
| R-501 | No SBOM/provenance pipeline yet; dependency advisory monitoring (cargo-audit) must run on a real pipeline. | Medium | MITIGATING | release eng | Updated 2026-07-17: `cargo audit` **has now been run** in dev and is wired into `.github/workflows/ci.yml` as a dedicated `sca` job over **both** Rust workspaces, failing on any advisory not documented in `docs/SECURITY_AUDIT_EXCEPTIONS.md`. fmt/clippy now cover the whole workspace + mls-core, and the backend CI job runs the api integration tests against a real Postgres service. **SBOM generation added 2026-07-18:** `scripts/generate_sbom.sh` produces a **CycloneDX 1.3** SBOM per package across all three Rust workspaces (via `cargo-cyclonedx`; verified locally — 220 components for the api), and a new CI `sbom` job generates + uploads them as build artifacts on every run. Residual (keeps this MITIGATING): build **provenance** / signed attestations (e.g. SLSA), publishing SBOMs with releases, and running the whole pipeline on the actual repo runner. |
| R-505 | OpenMLS's transitive `libcrux` crates carry RustSec advisories (`cargo audit`): 4 High (8.2) + 2 Medium + 2 unmaintained. | High→Low (see note) | ACCEPTED (tracked) | crypto integrator | Assessed 2026-07-17 (Gate 0 R-G0-1). **Corrected from the initial Gate 0 report, which overstated this.** For the `aarch64-apple-darwin` build with default features the active HPKE/AEAD backend is **RustCrypto** (`hpke-rs-rust-crypto`, `aes-gcm 0.10.3`), so `libcrux-aesgcm`/`libcrux-aead`/`libcrux-chacha20poly1305` are **not compiled** (false positives, verified via `cargo tree -i`). Only `libcrux-sha3 0.0.8` + `libcrux-secrets 0.0.5` compile, and the vulnerable **SHAKE / const-time-swap** paths are not invoked by the SHA-256/AES-GCM ciphersuite. No upstream fix is reachable: `hpke-rs 0.6.1` pins `libcrux-sha3 ^0.0.8` and `openmls_rust_crypto 0.5.1` is the latest release. Each ID is documented with a removal trigger in `docs/SECURITY_AUDIT_EXCEPTIONS.md`. **Review by 2026-10-17** or when upstream ships a release off the pinned libcrux; do not `[patch]`-override (API-breaking). Realized residual risk is Low given non-reachability; recorded as a tracked acceptance, not a dismissal. |
| R-502 | Signing keys, push credentials (APNs), and App Attest server config are not provisioned; no key-compromise playbook rehearsal has occurred. | High | OPEN | ops | Separate keys per environment; never in repo/CI-visible-to-forks. |
| R-503 | External penetration test, mobile assessment, infrastructure review, and cryptographic review have **not** been performed. | Critical | OPEN | security architect | Launch blocker per mission statement. |

## Accepted risks (explicit)

| ID | Risk | Owner | Rationale |
|----|------|-------|-----------|
| R-901 | Delete-for-everyone and disappearing messages cannot guarantee deletion on a recipient who has copied, exported, screenshotted, or backed up content. | product | Inherent to any messaging system; surfaced honestly in UI and PRIVACY.md, not hidden. |
| R-902 | Screenshots cannot be reliably prevented on iOS. | product | Platform limitation; we redact the app switcher and offer previews control, and state this honestly. |
| R-904 | **Secret ("view-once") messages** — the "cannot be seen again" claim must not be overstated, and multi-device consumption is not yet account-wide. | product + crypto integrator | **Feature landed 2026-07-18** (merged to main). View-once text runs through the REAL MLS pipeline (classification + body inside the ciphertext; relay stays blind — no server change); reveal is a crash-safe, fail-closed state machine in Rust (`mls_core::secret`/`content`/`durable`, `Sealed→Countdown 3s→Visible 10s→Consumed`), monotonic clock (system-clock changes can't extend), atomic reveal (a failed state write reveals nothing), crash-on-reveal fails closed on relaunch, replayed/duplicate/double-tap rejected, sender keeps no reopenable copy. Enforcement is Rust; the SwiftUI overlay is presentation-only. **Verified:** mls-core 10 + mls-ffi 6 secret tests, fake-clock + **real-core** view-model tests, bridge tests on host **and in the iOS 26.5 simulator**, a **runnable `@main` app** exercised through the full lifecycle in the simulator (screenshots in `docs/screenshots/`), and a **state-transition fuzzer** (`secret_state`, 4.2M+ execs — **found + fixed** a `now_ms`-overflow panic). **Honest limits (documented in `docs/SECRET_MESSAGES.md`):** "cannot be seen again" = cannot be reopened through SENTINEL; screenshots (detected, not prevented — R-902), external camera, compromised OS, and flash-forensic recovery are NOT prevented. **Multi-device: account-wide single-consumption LANDED 2026-07-18 (ADR-0015 Accepted).** Revealing on one device emits an E2EE, relay-blind `SecretConsumed` control message (new bounded/fuzzed content kind) that consumes the copy on the account's other devices — proven by a real 3-member-group mls-core simulation (phone reveals → tablet consumed, relay sees only ciphertext) + FFI/Swift fan-out tests. **Honest residual (inherent to relay-blindness, documented):** a sub-second concurrent open on two devices can briefly overlap (never a re-open), and an offline device is eventually-consistent (consumes on next sync). |
| R-903 | v1 was single-active-device; controlled multi-device now implemented (ADR-0008). | product | **Implemented 2026-07-18 (server side).** The single-active-device index is replaced (migration V13) by a per-account cap (`AuthService::MAX_ACTIVE_DEVICES = 8`) enforced atomically. A device is added ONLY through the **trusted-device enrollment ceremony**: an already-enrolled device signs a `DeviceEnroll` transcript authorizing the new device's key (`enroll_device_begin`/`_finish`; `POST /v1/devices/enroll/begin`+`/finish`), which provisions the new device a session — **password-only can never add a device** (proven: re-registering an existing username → 409). Device list + owner-scoped revoke (`GET /v1/devices`, `POST /v1/devices/revoke`) cascade token/family invalidation. `active_device_for_account` now returns the deterministic primary so login stays well-defined. Tested: `auth-core/tests/invariants.rs` (enroll/cap/badsig/revoked-authorizer/revoke-cascade), `services/api/tests/devices.rs` (end-to-end enroll→act→revoke, password-only denied), `pg_invariants.rs` (capped multi-device at the DB). ****Enrollment + recovery bindings are now logged in the transparency log (device additions are auditable — a server cannot add a device undetected). Still OPEN:** revocation-EVENT logging (additions are logged; a distinct revocation leaf type is future work); the on-device QR + short-authentication-string (SAS) pairing UX that defeats a MITM during the pubkey exchange (R-101); recovery-secret enrollment (the no-other-device path) lands with R-304; enrollment/revocation events are not yet recorded in the transparency log (ADR-0008 asks for it); software-signer devices are not yet blocked from *approving* enrollments. |

## How this file is used

- Every milestone report cross-references affected risk IDs.
- No item may move to `CLOSED` without a linked evidence artifact (test, audit letter, config).
- CI should fail a release build if any `Critical` risk is `OPEN` and not explicitly,
  time-box-accepted by the named owner.
